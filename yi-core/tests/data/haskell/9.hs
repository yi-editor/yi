-- comment 
instance Show Region where
    show r = show (regionStart r) ++ 
             (case regionDirection r of
             ) ++ 
             show (regionEnd r)
-- comment 
instance Show Region where
    show r = show (regionStart r) ++ 
             (case regionDirection r of
             ) ++ 
             show (regionEnd r)
-- comment 
instance Show Region where
    show r = show (regionStart r) ++ 
             (case regionDirection r of
             ) ++ 
             show (regionEnd r)
-- comment 
instance Show Region where
    show r = show (regionStart r) ++ 
             (case regionDirection r of
             ) ++ 
             show (regionEnd r)
-- comment 
instance Show Region where
    show r = show (regionStart r) ++ 
             (case regionDirection r of
             ) ++ 
             show (regionEnd r)
-- comment 
instance Show Region where
    show r = show (regionStart r) ++ 
             (case regionDirection r of
             ) ++ 
             show (regionEnd r)
-- comment 
instance Show Region where
    show r = show (regionStart r) ++ 
             (case regionDirection r of
             ) ++ 
             show (regionEnd r)
-- comment 
instance Show Region where
    show r = show (regionStart r) ++ 
             (case regionDirection r of
             ) ++ 
             show (regionEnd r)
-- comment 
instance Show Region where
    show r = show (regionStart r) ++ 
             (case regionDirection r of
             ) ++ 
             show (regionEnd r)
-- comment 
instance Show Region where
    show r = show (regionStart r) ++ 
             (case regionDirection r of
             ) ++ 
             show (regionEnd r)
-- comment 
instance Show Region where
    show r = show (regionStart r) ++ 
             (case regionDirection r of
             ) ++ 
             show (regionEnd r)
-- comment 
instance Show Region where
    show r = show (regionStart r) ++ 
             (case regionDirection r of
             ) ++ 
             show (regionEnd r)
-- comment 
instance Show Region where
    show r = show (regionStart r) ++ 
             (case regionDirection r of
             ) ++ 
             show (regionEnd r)
-- comment 
instance Show Region where
    show r = show (regionStart r) ++ 
             (case regionDirection r of
             ) ++ 
             show (regionEnd r)
-- comment 
instance Show Region where
    show r = show (regionStart r) ++ 
             (case regionDirection r of
             ) ++ 
             show (regionEnd r)
-- comment 
instance Show Region where
    show r = show (regionStart r) ++ 
             (case regionDirection r of
             ) ++ 
             show (regionEnd r)
-- comment 
instance Show Region where
    show r = show (regionStart r) ++ 
             (case regionDirection r of
             ) ++ 
             show (regionEnd r)
-- comment 
instance Show Region where
    show r = show (regionStart r) ++ 
             (case regionDirection r of
             ) ++ 
             show (regionEnd r)
-- comment 
instance Show Region where
    show r = show (regionStart r) ++ 
             (case regionDirection r of
             ) ++ 
             show (regionEnd r)
-- comment 
instance Show Region where
    show r = show (regionStart r) ++ 
             (case regionDirection r of
             ) ++ 
             show (regionEnd r)
-- comment 
instance Show Region where
    show r = show (regionStart r) ++ 
             (case regionDirection r of
             ) ++ 
             show (regionEnd r)
-- comment 
instance Show Region where
    show r = show (regionStart r) ++ 
             (case regionDirection r of
             ) ++ 
             show (regionEnd r)
-- comment 
instance Show Region where
    show r = show (regionStart r) ++ 
             (case regionDirection r of
             ) ++ 
             show (regionEnd r)
-- comment 
instance Show Region where
    show r = show (regionStart r) ++ 
             (case regionDirection r of
             ) ++ 
             show (regionEnd r)
-- comment 
instance Show Region where
    show r = show (regionStart r) ++ 
             (case regionDirection r of
             ) ++ 
             show (regionEnd r)
-- comment 
instance Show Region where
    show r = show (regionStart r) ++ 
             (case regionDirection r of
             ) ++ 
             show (regionEnd r)
-- comment 
instance Show Region where
    show r = show (regionStart r) ++ 
             (case regionDirection r of
             ) ++ 
             show (regionEnd r)
-- comment 
instance Show Region where
    show r = show (regionStart r) ++ 
             (case regionDirection r of
             ) ++ 
             show (regionEnd r)
-- comment 
instance Show Region where
    show r = show (regionStart r) ++ 
             (case regionDirection r of
             ) ++ 
             show (regionEnd r)
